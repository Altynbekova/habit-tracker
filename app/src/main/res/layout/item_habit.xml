<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="habitModel"
            type="com.example.habittracker.db.entity.HabitModel" />

        <variable
            name="clickListener"
            type="com.example.habittracker.ui.OnClickItemInterface" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:cardUseCompatPadding="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <View
                    android:id="@+id/viewColorIndicator"
                    android:layout_width="12dp"
                    android:layout_height="40dp"
                    android:layout_marginEnd="12dp"
                    android:background="@{habitModel.color}" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/textViewHabitName"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="@{habitModel.name}"
                        android:textSize="18sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/textViewHabitDescription"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="2"
                        android:text='@{habitModel.description, default="Описание привычки"}'
                        android:textSize="14sp" />

                </LinearLayout>

                <TextView
                    android:id="@+id/textViewStreak"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/streak_background"
                    android:padding="8dp"
                    android:text="@{String.valueOf(habitModel.currentStreak)}"
                    android:textSize="16sp"
                    android:textStyle="bold" />

            </LinearLayout>

            <!-- Дни недели в одной строке -->
            <LinearLayout
                android:id="@+id/layoutWeekDays"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="12dp"
                android:gravity="center"
                android:orientation="horizontal">

                <!-- Days will be added dynamically -->
            </LinearLayout>

            <!-- Кнопки в одной строке -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal"
                android:weightSum="3">

                <Button
                    android:id="@+id/buttonComplete"
                    android:layout_width="0dp"
                    android:layout_height="40dp"
                    android:layout_marginEnd="4dp"
                    android:layout_weight="1"
                    android:background="#C7CEEA"
                    android:onClick="@{() -> clickListener.onCompleteItem(habitModel)}"
                    android:text="@string/done"
                    android:textSize="11sp"
                    android:textStyle="bold" />

                <Button
                    android:id="@+id/buttonEdit"
                    android:layout_width="0dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="4dp"
                    android:layout_marginEnd="4dp"
                    android:layout_weight="1"
                    android:background="#E6E6FA"
                    android:onClick="@{() -> clickListener.onClickItem(habitModel, true)}"
                    android:text="@string/change"
                    android:textSize="11sp" />

                <Button
                    android:id="@+id/buttonDelete"
                    android:layout_width="0dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="4dp"
                    android:layout_weight="1"
                    android:background="#FFB6C1"
                    android:onClick="@{() -> clickListener.onClickItem(habitModel, false)}"
                    android:text="@string/delete"
                    android:textSize="11sp" />

            </LinearLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>